<p style="color: green"><%= notice %></p>

<h2 class="text-center">Mascotas</h2>
<%= link_to "Nueva Mascota", new_pet_path %>

<table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Nombre</th>
      <th scope="col">Raza</th>
      <th scope="col">Cumpleaños</th>
      <th scope="col">Cliente</th>
      <th scope="col">Visitas</th>

      <th colspan="2"></th>
    </tr>
  </thead>
  <tbody>
    <% @pets.each do |pet| %>
    <tr>
      <td><%= pet.name %></td>
      <td><%= pet.race %></td>
      <td><%= pet.birthday %></td>
      <td><%= pet.Client.name %></td>
      <td><%= pet.pet_histories.count() %></td>
      <td><%= link_to "Mostrar", pet %> </td>
      <td><%= link_to "Editar", edit_pet_path(pet.id) %></td>
    </tr>
    <% end %>
  </tbody>
</table>